#include "main.h"

/**
*print-alphabet-entry point
*
*Description:points the alphabet with _putchar
*
*Return:void
*/

void print-alphabet(void)
{           char c;

            for (c='a';c<='z';c++
            {
                    _putchar(c);
            }
            _putchar('/n');
}
